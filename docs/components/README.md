# RESERVED — Phase 5: Component Library Specification

This directory is reserved for **Phase 5 — Component Library Specification**
documents (Doc-ID prefix `CMP-NNN`, contract atoms `CP-NNN`).

Phase 5 is **NOT YET OPEN**. Per GOV-001 §5.2, no content may be added here until
Phase 4 is FROZEN and Phase 5 is declared open in RDM-001.

Planned deliverables (see RDM-001 §2): the design language and component contracts
as specification documents — no code, no HTML, no CSS (those belong to
implementation steps 7+, after documentation freeze).
