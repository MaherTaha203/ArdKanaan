# RESERVED — Phase 6: Screen Blueprints

This directory is reserved for **Phase 6 — Screen Blueprints** documents
(Doc-ID prefix `SCR-NNN`, requirement atoms `SC-NNN`).

Phase 6 is **NOT YET OPEN**. Per GOV-001 §5.2, no content may be added here until
Phase 5 is FROZEN and Phase 6 is declared open in RDM-001.

Planned deliverables (see RDM-001 §2): screen-by-screen blueprints composing UX
rules (`UX-NNN`), component contracts (`CP-NNN`), and data model rules (`DB-NNN`) —
as specification documents only. The HTML prototype is implementation step 7,
after documentation freeze.
