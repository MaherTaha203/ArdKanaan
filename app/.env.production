# Public client config for the production build (read at build time by Vite).
# These are PUBLIC by design and ship in the browser bundle:
#   - the project URL
#   - the publishable (anon) key
# Real access control is Postgres RLS (authenticated-only). NEVER put the
# service_role key or the database password here.
VITE_SUPABASE_URL=https://ffolzbxofmretqjoycbm.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_RFeUzOYz2mlnC-FGl3jhhw_w8Y9E_uM
